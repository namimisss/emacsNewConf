;; =============================================================================
;; config/ui/ui-config.el - 界面配置模块入口
;; =============================================================================
;; 此文件是ui目录下所有配置的统一入口

;; 1. 主题配置 (图标、主题、状态栏等)
(require 'ui-themes)

;; 2. 界面组件配置 (补全界面、边距等)
(require 'ui-interface)

;; 提供ui配置入口
(provide 'ui-config) 